name: Build and Run Example

on:
  push:
    branches: [main]
    tags:
      - "*"
  pull_request:
    branches: [main]

jobs:
  build-and-run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest, windows-11-arm]
    env:
      AIC_SDK_LICENSE: ${{ secrets.AIC_SDK_LICENSE }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build ./example

      - name: Build
        run: cmake --build build -j

      - name: Download model (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          mkdir -p build/models
          curl -L -o build/models/quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel \
          https://artifacts.ai-coustics.io/models/quail-vf-2-1-s-16khz/v3/quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel

      - name: Download model (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path build\\models | Out-Null
          Invoke-WebRequest -Uri "https://artifacts.ai-coustics.io/models/quail-vf-2-1-s-16khz/v3/quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel" -OutFile "build\\models\\quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel"

      - name: Run Example (Linux/macOS)
        if: runner.os != 'Windows'
        run: ./build/my_app build/models/quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel

      - name: Run Example (Windows)
        if: runner.os == 'Windows'
        run: .\build\Debug\my_app.exe build\models\quail_vf_2_1_s_16khz_5i8jb8of_v12.aicmodel
